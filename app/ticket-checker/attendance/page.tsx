'use client';
import { useCallback, useEffect, useState } from 'react';
type Row = { _id: string; ticketNumber: string; ticketType: string; checkedInAt?: string; user?: { name?: string }; event?: { title?: string }; checkedInBy?: { name?: string } };
export default function AttendancePage() { const [items, setItems] = useState<Row[]>([]); const [search, setSearch] = useState(''); const [date, setDate] = useState(''); const [error, setError] = useState(''); const [loading, setLoading] = useState(true); const load = useCallback(async () => { setLoading(true); const params = new URLSearchParams({ view: 'attendance' }); if (search) params.set('search', search); if (date) params.set('date', date); try { const res = await fetch(`/api/checkin?${params}`); const json = await res.json(); if (!res.ok || !json.success) throw new Error(json.error?.message); setItems(json.data.items ?? []); } catch { setError('Unable to load attendance records.'); } finally { setLoading(false); } }, [search, date]); useEffect(() => { void load(); }, [load]); return <section className="mx-auto max-w-7xl px-4 py-10 font-cause text-text-dark"><h1 className="text-3xl font-dynapuff font-semibold">Attendance Log</h1><div className="my-6 grid gap-3 md:grid-cols-3"><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Ticket number or type" className="rounded-lg border border-border px-3 py-2" /><input type="date" value={date} onChange={(e) => setDate(e.target.value)} className="rounded-lg border border-border px-3 py-2" /><button onClick={() => void load()} className="rounded-lg bg-primary px-4 py-2 text-white">Filter</button></div>{error && <p className="text-error">{error}</p>}{loading ? <p>Loading attendance…</p> : <div className="overflow-x-auto rounded-xl border border-border bg-surface"><table className="w-full min-w-[780px] text-left"><thead><tr className="border-b border-divider text-sm text-text-light"><th className="p-3">Ticket</th><th className="p-3">Attendee</th><th className="p-3">Event</th><th className="p-3">Type</th><th className="p-3">Checked In At</th><th className="p-3">Checker</th></tr></thead><tbody>{items.length === 0 ? <tr><td colSpan={6} className="p-5 text-text-light">No attendance records yet.</td></tr> : items.map((item) => <tr key={item._id} className="border-b border-divider last:border-0"><td className="p-3">{item.ticketNumber}</td><td className="p-3">{item.user?.name ?? '-'}</td><td className="p-3">{item.event?.title ?? '-'}</td><td className="p-3">{item.ticketType}</td><td className="p-3">{item.checkedInAt ? new Date(item.checkedInAt).toLocaleString() : '-'}</td><td className="p-3">{item.checkedInBy?.name ?? '-'}</td></tr>)}</tbody></table></div>}</section>; }
