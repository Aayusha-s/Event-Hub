import TicketLookup from "@/components/TicketLookup";
export default function CheckTicketPage() {
  return (
    <TicketLookup
      title="Check Ticket"
      description="Verify a ticket before confirming check-in."
    />
  );
}
