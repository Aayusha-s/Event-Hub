"use client";
import { useEffect, useState } from "react";
type Vendor = { _id: string; businessName: string; category: string; approvalStatus: string; owner?: { name?: string; email?: string } };
type Event = { _id: string; title: string; venue: string; startDate: string; approvalStatus: string; organizer?: { name?: string } };
type Stall = { vendorId: string; vendor: { businessName: string; category: string; owner?: { name?: string; email?: string } }; event?: { _id: string; title?: string }; stallName?: string; status: string };
const request = async (url: string, body: unknown) => { const r = await fetch(url, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const j = await r.json(); if (!r.ok || !j.success) throw new Error(j.error?.message || "Approval update failed."); };
export default function ApprovalsPage() { const [vendors,setVendors]=useState<Vendor[]>([]),[events,setEvents]=useState<Event[]>([]),[stalls,setStalls]=useState<Stall[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true); const load=async()=>{setLoading(true);try{const [v,e,s]=await Promise.all([fetch('/api/admin/vendors?status=pending'),fetch('/api/admin/events?approvalStatus=pending&pageSize=100'),fetch('/api/admin/stalls?status=pending')]);const [vj,ej,sj]=await Promise.all([v.json(),e.json(),s.json()]);if(!v.ok||!e.ok||!s.ok)throw new Error(vj.error?.message||ej.error?.message||sj.error?.message||'Unable to load approvals.');setVendors(vj.data.items??[]);setEvents(ej.data.items??[]);setStalls(sj.data.items??[]);setError('');}catch(e){setError(e instanceof Error?e.message:'Unable to load approvals.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const act=async(url:string,status:string)=>{try{await request(url,{approvalStatus:status});await load()}catch(e){setError(e instanceof Error?e.message:'Approval update failed.')}};const actions=(approve:()=>void,reject:()=>void)=><div className="flex gap-2"><button onClick={approve} className="rounded bg-green-600 px-3 py-1 text-sm text-white">Approve</button><button onClick={reject} className="rounded bg-red-600 px-3 py-1 text-sm text-white">Reject</button></div>;return <section className="mx-auto max-w-7xl px-4 py-12 font-cause text-text-dark"><h1 className="font-dynapuff text-3xl font-semibold">Pending Approvals</h1><p className="mt-1 text-text-light">Review vendor applications, organizer events, and vendor stall requests.</p>{error&&<p className="mt-4 text-error">{error}</p>}{loading?<p className="mt-6">Loading approvals...</p>:<div className="mt-6 space-y-8"><section><h2 className="mb-3 text-xl font-semibold">Vendor Applications ({vendors.length})</h2><div className="overflow-x-auto rounded-xl border border-border"><table className="w-full min-w-[650px]"><thead><tr><th className="p-3 text-left">Vendor</th><th className="p-3 text-left">Contact</th><th className="p-3 text-left">Category</th><th className="p-3 text-left">Action</th></tr></thead><tbody>{vendors.length?vendors.map(v=><tr key={v._id} className="border-t border-divider"><td className="p-3">{v.businessName}</td><td className="p-3">{v.owner?.name}<br/><span className="text-sm text-text-light">{v.owner?.email}</span></td><td className="p-3">{v.category}</td><td className="p-3">{actions(()=>void act(`/api/admin/vendors/${v._id}`,'approved'),()=>void act(`/api/admin/vendors/${v._id}`,'rejected'))}</td></tr>):<tr><td className="p-3 text-text-light" colSpan={4}>No pending vendor applications.</td></tr>}</tbody></table></div></section><section><h2 className="mb-3 text-xl font-semibold">Event Approvals ({events.length})</h2><div className="overflow-x-auto rounded-xl border border-border"><table className="w-full min-w-[650px]"><thead><tr><th className="p-3 text-left">Event</th><th className="p-3 text-left">Organizer</th><th className="p-3 text-left">Date / location</th><th className="p-3 text-left">Action</th></tr></thead><tbody>{events.length?events.map(e=><tr key={e._id} className="border-t border-divider"><td className="p-3">{e.title}</td><td className="p-3">{e.organizer?.name}</td><td className="p-3">{new Date(e.startDate).toLocaleString()}<br/><span className="text-sm text-text-light">{e.venue}</span></td><td className="p-3">{actions(()=>void act(`/api/admin/events/${e._id}`,'approved'),()=>void act(`/api/admin/events/${e._id}`,'rejected'))}</td></tr>):<tr><td className="p-3 text-text-light" colSpan={4}>No pending events.</td></tr>}</tbody></table></div></section><section><h2 className="mb-3 text-xl font-semibold">Stall Requests ({stalls.length})</h2><div className="overflow-x-auto rounded-xl border border-border"><table className="w-full min-w-[650px]"><thead><tr><th className="p-3 text-left">Stall</th><th className="p-3 text-left">Vendor</th><th className="p-3 text-left">Event</th><th className="p-3 text-left">Action</th></tr></thead><tbody>{stalls.length?stalls.map(s=>s.event&&<tr key={`${s.vendorId}-${s.event._id}`} className="border-t border-divider"><td className="p-3">{s.stallName??'Vendor stall'}</td><td className="p-3">{s.vendor.businessName}<br/><span className="text-sm text-text-light">{s.vendor.category}</span></td><td className="p-3">{s.event.title}</td><td className="p-3">{actions(()=>void act(`/api/admin/stalls/${s.vendorId}/${s.event!._id}`,'approved'),()=>void act(`/api/admin/stalls/${s.vendorId}/${s.event!._id}`,'rejected'))}</td></tr>):<tr><td className="p-3 text-text-light" colSpan={4}>No pending stall requests.</td></tr>}</tbody></table></div></section></div>}</section>; }
