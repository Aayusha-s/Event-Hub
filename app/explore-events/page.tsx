'use client';
import { Suspense, useEffect, useState } from 'react'; import { useRouter, useSearchParams } from 'next/navigation'; import EventCard from '@/components/EventCard'; import Pagination from '@/components/Pagination'; import { ListFilter } from 'lucide-react';
type Event={_id:string;title:string;description:string;venue:string;category:string;tags:string[];images:string[];ticketTypes:{price:number}[];organizer?:{name?:string}}; type Result={items:Event[];pagination:{page:number;totalPages:number;total:number}};
function Content(){const params=useSearchParams(),router=useRouter(),[result,setResult]=useState<Result|null>(null),[loading,setLoading]=useState(true),[showFilters,setShowFilters]=useState(false);const update=(key:string,value:string)=>{const q=new URLSearchParams(params.toString());if(value)q.set(key,value);else q.delete(key);q.set('page','1');router.push(`/explore-events?${q}`)};useEffect(()=>{const q=new URLSearchParams(params.toString());q.set('status','published');q.set('pageSize',q.get('pageSize')??'12');if(q.get('query')){q.set('search',q.get('query')!);q.delete('query')}fetch(`/api/events?${q}`).then(r=>r.json()).then(r=>{if(r.success)setResult(r.data)}).finally(()=>setLoading(false))},[params]);const page=Number(params.get('page')??'1');return <section className='text-text-dark my-10 mx-5 px-4'><div className='flex flex-row items-center'><h2 className='text-3xl font-semibold text-text-dark font-dynapuff'>Explore Events</h2></div><div className='flex justify-end gap-4 mb-4 mt-2 cursor-pointer'><ListFilter onClick={()=>setShowFilters(!showFilters)}/></div>{showFilters&&<div className='flex gap-4 mb-6 p-4 border border-brown-normal rounded-xl bg-brown-light'><select value={params.get('sort')??''} onChange={e=>update('sort',e.target.value)} className='border border-brown-normal rounded-lg px-3 py-2 bg-white text-sm'><option value=''>Default</option><option value='newest'>Newest</option><option value='oldest'>Oldest</option><option value='trending'>Trending</option><option value='popular'>Most Popular</option><option value='rating'>Highest Rated</option><option value='priceAsc'>Price Low to High</option><option value='priceDesc'>Price High to Low</option></select><select value={params.get('availability')??''} onChange={e=>update('availability',e.target.value)} className='border border-brown-normal rounded-lg px-3 py-2 bg-white text-sm'><option value=''>All availability</option><option value='available'>Available</option><option value='soldOut'>Sold Out</option></select></div>}{loading?<p>Loading events…</p>:<><p className='mb-4'>{result?.pagination.total??0} results</p><div className='grid grid-cols-1 gap-y-9 gap-x-17 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 lg:gap-3 xl:grid-cols-4 2xl:grid-cols-4'>{result?.items.map(event=><EventCard key={event._id} eventId={event._id} tags={event.tags} imageUrl={event.images[0]??'/images/party.png'} imageAlt={event.title} title={event.title} organizer={`By ${event.organizer?.name??'Event organizer'}`} descriptions={[event.description]} location={event.venue} price={event.ticketTypes.some(ticket=>ticket.price===0)?'Free':`From Rs.${Math.min(...event.ticketTypes.map(ticket=>ticket.price))}`}/>)}</div><div className='mt-6'><Pagination page={page} totalPages={result?.pagination.totalPages} onPageChange={next=>update('page',String(next))}/></div></>}</section>}
export default function Page(){return <Suspense fallback={<div className='px-5 py-10 text-text-muted'>Loading events…</div>}><Content/></Suspense>}
