'use client';
import Button from '@/components/Button'; import EventCard from '@/components/EventCard'; import Pagination from '@/components/Pagination'; import { TrendingUp } from 'lucide-react'; import { useEffect, useState } from 'react'; import { useSearchParams } from 'next/navigation';
type Event={_id:string;title:string;description:string;venue:string;tags:string[];images:string[];ticketTypes:{price:number}[];organizer?:{name?:string}}; type Tag={name:string;count:number};
export default function Page(){const params=useSearchParams(),tag=params.get('tag')??'';const [tags,setTags]=useState<Tag[]>([]),[events,setEvents]=useState<Event[]>([]);useEffect(()=>{fetch('/api/events/taxonomy').then(r=>r.json()).then(r=>{if(r.success)setTags(r.data.tags)});const q=new URLSearchParams({status:'published',pageSize:'24'});if(tag)q.set('tags',tag);fetch(`/api/events?${q}`).then(r=>r.json()).then(r=>{if(r.success)setEvents(r.data.items)})},[tag]);return <section className='flex flex-col my-4 mx-2 px-4 font-cause text-text-dark md:my-3 md:mx-3 md:px-3 lg:my-4 lg:mx-4 lg:px-4 xl:my-6 xl:mx-6 xl:px-6 2xl:my-8 2xl:mx-8 2xl:px-8'><div className="flex flex-col mb-2"><h1 className='font-dynapuff text-2xl md:text-3xl font-bold'>Browse Events by Tags</h1></div><div className='bg-brown-light w-full rounded-xl mb-4 p-4 space-y-2'><div className='flex flex-row items-center gap-2'><TrendingUp/><h3 className='text-xl font-semibold mb-2'>Trending Now</h3></div><div className='flex flex-wrap gap-2'>{tags.map(item=><a key={item.name} href={`/event-tags?tag=${encodeURIComponent(item.name)}`}><Button text={`${item.name} (${item.count})`} variant='tag'/></a>)}</div></div>{tag&&<p className='mb-4'>Active tag: {tag}</p>}<h3 className='font-bold mb-4'>All Events</h3><div className='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4'>{events.map(event=><EventCard key={event._id} eventId={event._id} tags={event.tags} imageUrl={event.images[0]??'/images/party.png'} imageAlt={event.title} title={event.title} organizer={`By ${event.organizer?.name??'Event organizer'}`} descriptions={[event.description]} location={event.venue} price={event.ticketTypes.some(t=>t.price===0)?'Free':`From Rs.${Math.min(...event.ticketTypes.map(t=>t.price))}`}/>)}</div><div className='mt-7'><Pagination/></div></section>}
