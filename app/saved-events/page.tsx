'use client';
import { useCallback, useEffect, useState } from 'react'; import SavedEventsCard from '@/components/SavedEventsCard'; import Button from '@/components/Button';
type Event={_id:string;title:string;venue:string;startDate:string;category:string;ticketTypes:{price:number}[]}; type Item={event:Event};
export default function Page(){const [items,setItems]=useState<Item[]>([]),[page,setPage]=useState(1),[pages,setPages]=useState(1),[q,setQ]=useState(''),[category,setCategory]=useState(''),[loading,setLoading]=useState(true);const load=useCallback(async()=>{setLoading(true);const r=await fetch(`/api/saved-events?page=${page}&pageSize=12&q=${encodeURIComponent(q)}&category=${encodeURIComponent(category)}`),j=await r.json();if(j.success){setItems(j.data.items);setPages(j.data.totalPages)}setLoading(false)},[page,q,category]);useEffect(()=>{const i=setTimeout(load,200);return()=>clearTimeout(i)},[load]);const remove=async(eventId:string)=>{await fetch('/api/saved-events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({eventId})});load()};return <section className='flex flex-col my-4 mx-2 px-4 font-cause text-text-dark md:mx-3 lg:mx-4 xl:mx-6'><h1 className='font-dynapuff text-2xl md:text-3xl font-bold'>Saved Events</h1><div className='mt-5 flex flex-wrap gap-3'><input value={q} onChange={e=>{setQ(e.target.value);setPage(1)}} placeholder='Search saved events...' className='rounded-lg border border-brown-normal px-3 py-2'/><input value={category} onChange={e=>{setCategory(e.target.value);setPage(1)}} placeholder='Filter category' className='rounded-lg border border-brown-normal px-3 py-2'/></div>{loading?<div className='mt-6 grid grid-cols-3 gap-4'>{[1,2,3].map(i=><div key={i} className='h-48 animate-pulse rounded-xl bg-brown-light'/>)}</div>:items.length===0?<p className='mt-6 rounded-xl border border-brown-normal p-6 text-center'>No saved events found.</p>:<div className='border border-brown-normal rounded-xl mt-6 p-4 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6'>{items.map(({event})=><SavedEventsCard key={event._id} eventId={event._id} title={event.title} date={new Date(event.startDate).toLocaleDateString()} location={event.venue} price={Math.min(...event.ticketTypes.map(t=>t.price))} onRemove={()=>remove(event._id)}/>)}</div>}<div className='mt-6 flex justify-between'><Button text='Previous' size='sm' variant='secondary' disabled={page===1} onClick={()=>setPage(page-1)}/><span>Page {page} of {pages||1}</span><Button text='Next' size='sm' variant='secondary' disabled={page>=pages} onClick={()=>setPage(page+1)}/></div></section>}
