'use client';
import { useEffect, useState } from 'react'; import { useParams, useRouter } from 'next/navigation'; import TicketPopup from '@/components/TicketPopup';
type Ticket={_id:string;ticketNumber:string;ticketType:string;qrCode:string;paymentStatus:string;ticketStatus:string;checkedIn:boolean;purchaseDate:string;event:{_id:string;title:string;venue:string;startDate:string;endDate:string;images?:string[];organizer?:{name?:string}}};
export default function Page(){const {id}=useParams<{id:string}>(),router=useRouter();const [ticket,setTicket]=useState<Ticket|null>(null),[error,setError]=useState('');useEffect(()=>{if(!id)return;fetch(`/api/tickets/${id}`).then(async response=>{const result=await response.json();if(!response.ok||!result.success)throw new Error(result.error?.message??'Ticket not found.');setTicket(result.data)}).catch(err=>setError(err.message))},[id]);if(error)return <section className='my-4 mx-2 px-4 font-cause text-text-dark'>{error}</section>;if(!ticket)return <section className='my-4 mx-2 px-4 font-cause text-text-dark'>Loading ticket…</section>;return <TicketPopup isOpen onClose={()=>router.push(`/event-details/${ticket.event._id}`)} ticket={ticket} onCancelled={()=>router.refresh()}/>}
