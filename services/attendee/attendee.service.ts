import { Types } from "mongoose";
import dbConnect from "@/lib/mongodb";
import Event from "@/models/Event";
import Ticket from "@/models/Ticket";
import SavedEvent from "@/models/SavedEvent";
import Review from "@/models/Review";
import Comment from "@/models/Comment";
import { HttpError } from "@/utils/api/httpError";

const eventLookup = [{ $lookup: { from: "events", localField: "event", foreignField: "_id", as: "event" } }, { $unwind: { path: "$event", preserveNullAndEmptyArrays: false } }, { $lookup: { from: "users", localField: "event.organizer", foreignField: "_id", as: "organizer" } }, { $unwind: { path: "$organizer", preserveNullAndEmptyArrays: true } }];
export const toggleSavedEvent = async (user: Types.ObjectId, event: Types.ObjectId) => { await dbConnect(); if (!await Event.exists({ _id: event })) throw new HttpError(404, "Event not found.", "NOT_FOUND"); const existing = await SavedEvent.findOne({ user, event }); if (existing) { await existing.deleteOne(); return { saved: false }; } await SavedEvent.create({ user, event }); return { saved: true }; };
export const getSavedEvents = async (user: Types.ObjectId) => SavedEvent.aggregate([{ $match: { user } }, ...eventLookup, { $match: { "event.status": { $ne: "cancelled" } } }, { $project: { createdAt: 1, event: { _id: "$event._id", title: "$event.title", venue: "$event.venue", images: "$event.images", startDate: "$event.startDate", endDate: "$event.endDate", category: "$event.category", tags: "$event.tags", ticketTypes: "$event.ticketTypes", organizer: { _id: "$organizer._id", name: "$organizer.name" } } } }, { $sort: { createdAt: -1 } }]).exec();
const attended = (user: Types.ObjectId, event: Types.ObjectId) => Ticket.exists({ user, event, checkedIn: true, ticketStatus: "active", paymentStatus: "paid" });
export const upsertReview = async (user: Types.ObjectId, event: Types.ObjectId, rating: number, text: string) => { await dbConnect(); if (!await attended(user, event)) throw new HttpError(403, "Only attendees who checked in may review this event.", "ATTENDANCE_REQUIRED"); return Review.findOneAndUpdate({ user, event }, { rating, text }, { new: true, upsert: true, runValidators: true, setDefaultsOnInsert: true }); };
export const deleteReview = async (user: Types.ObjectId, id: Types.ObjectId) => { await dbConnect(); const review = await Review.findOneAndDelete({ _id: id, user }); if (!review) throw new HttpError(404, "Review not found.", "NOT_FOUND"); };
export const getReviews = async (event: Types.ObjectId) => Review.aggregate([{ $match: { event } }, { $lookup: { from: "users", localField: "user", foreignField: "_id", as: "user" } }, { $unwind: "$user" }, { $project: { rating: 1, text: 1, createdAt: 1, updatedAt: 1, user: { _id: "$user._id", name: "$user.name", profileImage: "$user.profileImage" } } }, { $sort: { createdAt: -1 } }]).exec();
export const getReviewSummary = async (event: Types.ObjectId) => { const [summary] = await Review.aggregate([{ $match: { event } }, { $group: { _id: null, averageRating: { $avg: "$rating" }, reviewCount: { $sum: 1 } } }]); return { averageRating: summary?.averageRating ?? 0, reviewCount: summary?.reviewCount ?? 0 }; };
export const listComments = async (event: Types.ObjectId) => Comment.aggregate([{ $match: { event } }, { $lookup: { from: "users", localField: "user", foreignField: "_id", as: "user" } }, { $unwind: "$user" }, { $project: { event: 1, parent: 1, text: 1, likes: 1, dislikes: 1, createdAt: 1, updatedAt: 1, user: { _id: "$user._id", name: "$user.name", profileImage: "$user.profileImage" } } }, { $sort: { createdAt: 1 } }]).exec();
export const createComment = async (user: Types.ObjectId, event: Types.ObjectId, text: string, parent?: Types.ObjectId) => { await dbConnect(); if (parent && !await Comment.exists({ _id: parent, event })) throw new HttpError(400, "Reply target is invalid.", "VALIDATION_ERROR"); return Comment.create({ user, event, text, parent }); };
export const updateComment = async (user: Types.ObjectId, id: Types.ObjectId, text: string) => { const comment = await Comment.findOneAndUpdate({ _id: id, user }, { text }, { new: true, runValidators: true }); if (!comment) throw new HttpError(404, "Comment not found.", "NOT_FOUND"); return comment; };
export const deleteComment = async (user: Types.ObjectId, id: Types.ObjectId) => { const comment = await Comment.findOne({ _id: id, user }); if (!comment) throw new HttpError(404, "Comment not found.", "NOT_FOUND"); await Comment.deleteMany({ $or: [{ _id: id }, { parent: id }] }); };
export const reactToComment = async (user: Types.ObjectId, id: Types.ObjectId, reaction: "like" | "dislike") => { const opposite = reaction === "like" ? "dislikes" : "likes"; const target = reaction === "like" ? "likes" : "dislikes"; const comment = await Comment.findByIdAndUpdate(id, { $pull: { [opposite]: user }, $addToSet: { [target]: user } }, { new: true }); if (!comment) throw new HttpError(404, "Comment not found.", "NOT_FOUND"); return comment; };
export const getAttendeeDashboard = async (user: Types.ObjectId) => { await dbConnect(); const now = new Date(); const [tickets, savedEvents, reviews] = await Promise.all([Ticket.aggregate([{ $match: { user } }, ...eventLookup, { $project: { ticketNumber: 1, ticketType: 1, qrCode: 1, paymentStatus: 1, ticketStatus: 1, checkedIn: 1, purchaseDate: 1, event: { _id: "$event._id", title: "$event.title", venue: "$event.venue", images: "$event.images", startDate: "$event.startDate", endDate: "$event.endDate", category: "$event.category", organizer: { name: "$organizer.name" } } } }, { $sort: { purchaseDate: -1 } }]), getSavedEvents(user), Review.aggregate([{ $match: { user } }, ...eventLookup, { $project: { rating: 1, text: 1, createdAt: 1, event: { _id: "$event._id", title: "$event.title" } } }, { $sort: { createdAt: -1 } }])]); const active = tickets.filter(t => t.ticketStatus === "active" && t.paymentStatus === "paid"); const upcoming = active.filter(t => new Date(t.event.startDate) >= now); const past = active.filter(t => new Date(t.event.endDate) < now); const spending = await Ticket.aggregate([{ $match: { user, paymentStatus: "paid", ticketStatus: "active" } }, { $lookup: { from: "bookings", localField: "booking", foreignField: "_id", as: "booking" } }, { $unwind: { path: "$booking", preserveNullAndEmptyArrays: true } }, { $group: { _id: null, total: { $sum: { $ifNull: ["$booking.totalAmount", 0] } } } }]); return { upcoming, past, activeTickets: active, savedEvents, recentBookings: tickets.slice(0, 5), recentReviews: reviews.slice(0, 5), totalEventsAttended: active.filter(t => t.checkedIn).length, favoriteCategories: [...new Set(active.map(t => t.event.category))], favoriteOrganizers: [...new Set(active.map(t => t.event.organizer?.name).filter(Boolean))], spending: spending[0]?.total ?? 0 }; };
