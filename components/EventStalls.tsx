"use client";
import { useEffect, useState } from "react";
import { Store } from "lucide-react";
type Stall = { vendorId: string; businessName: string; category: string; logo?: string; stallName?: string };
export default function EventStalls({ eventId }: { eventId: string }) { const [items,setItems]=useState<Stall[]>([]); useEffect(()=>{fetch(`/api/events/${eventId}/stalls`,{cache:"no-store"}).then(async r=>{const j=await r.json();if(r.ok&&j.success)setItems(j.data??[])}).catch(()=>undefined)},[eventId]); if(!items.length)return null; return <section className="mt-10 md:mt-16"><h2 className="mb-4 font-dynapuff text-xl font-bold">Approved Event Stalls</h2><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{items.map(item=><article key={`${item.vendorId}-${item.stallName??"stall"}`} className="rounded-xl border-2 border-brown-normal p-4"><div className="flex items-center gap-3">{item.logo?<img src={item.logo} alt="" className="h-12 w-12 rounded-full object-cover"/>:<div className="rounded-full bg-brown-light p-3"><Store size={20}/></div>}<div><h3 className="font-semibold">{item.stallName??item.businessName}</h3><p className="text-sm text-text-light">{item.businessName} · {item.category}</p></div></div></article>)}</div></section>; }
